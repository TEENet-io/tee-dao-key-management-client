{
  "name": "@teenet/tee-dao-key-management-client",
  "version": "1.0.0",
  "description": "TEE DAO Key Management Client for TypeScript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "start": "node --no-deprecation dist/example.js",
    "example": "npm run build && npm run start"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.11.0",
    "@grpc/proto-loader": "^0.7.13",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist/**/*"
  ],
  "keywords": [
    "tee",
    "dao",
    "key-management",
    "grpc",
    "typescript"
  ],
  "license": "PROPRIETARY",
  "private": true
}